/**
 * actionType.js - Action Types for Redux/Reducer
 *
 * Defines all possible action types used in the application's state reducer.
 * These constants are used in both components (dispatch) and the reducer (switch cases)
 * to maintain consistency and prevent typos.
 */

export const Types = {
  ADD_TO_BASKET: "ADD_TO_CART", // Action to add a product to the shopping basket
  REMOVE_FROM_BASKET: "REMOVE_FROM_CART",
};
